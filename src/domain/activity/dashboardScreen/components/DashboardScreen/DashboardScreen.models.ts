/**
 * All the reports that will be available
 */
export enum Reports {
  TimeEntries = "timeEntries",
  HoresLogged = "horesLogged",
  AverageLoggedTimePerEntry = "averageEndTime",
}

/**
 * Period of reports
 */
export enum ReportPeriod {
  Daily = "daily",
  Weekly = "weekly",
  Monthly = "Monthly",
}
